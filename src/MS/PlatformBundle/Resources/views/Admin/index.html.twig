{% extends "@MSPlatform/Admin/homepage.html.twig" %}

{% block title %}{{ parent() }} - Index{% endblock %}

{% block body_university %}
<div class="container">
    <h2>Filière
        <a class="btn btn-default" href="{{ path('ms_admin_addCourse') }}">
            <i class="glyphicon glyphicon-plus"></i>
            Ajouter
        </a>
    </h2>



    <table class="table table-striped">
        <thead>
        <tr>
            {#<th>ID</th>#}
            <th>TYPE</th>
            <th>FILIERE</th>
            <th>ANNEE SCOLAIRE</th>
            <th>MATIERES</th>
            <th>COEFFICIENT TOTAL</th>
            <th>NOMBRE DE MATIERE</th>
            <th>ETUDIANTS</th>
            <th>NOMBRE D'ETUDIANT</th>
        </tr>
        </thead>
        <div class="row">
        <tbody>
        {% for course in courseslist %}
            <tr>
                <td>{{ course.type }}</td>
                <td>{{ course.name }}</td>
                <td>{{ course.dateStart|date('m/Y') }}-{{ course.dateEnd|date('m/Y') }}</td>

                <td>

                    <table>
                    {% for subject in course.subjects %}
                        <tr>
                            {% if course.subjects is empty %}
                                <td>Vide</td>
                            {% else %}
                                <td>{{ subject.name }}{% if not loop.last %}, {% endif %}</td>
                            {% endif %}
                        </tr>
                    {% endfor %}
                    </table>
                </td>
                <td>{{ course.totalCoef }}</td>
                <td>{{ course.totalSubjects }}</td>
                <td>

                    <table>
                        {% for student in course.students %}
                            <tr>
                                {% if course.students is empty %}
                                    <td>Vide</td>
                                {% else %}
                                    <td>{{ student.firstname }} {{ student.lastname }}{% if not loop.last %}, {% endif %}</td>
                                {% endif %}
                            </tr>
                        {% endfor %}
                    </table>
                </td>
                <td>{{ course.totalStudents }}</td>
                <td>
                    <a class="btn btn-default" href="{{ path('ms_admin_editCourse', {'id': course.id}) }}">
                        <i class="glyphicon glyphicon-edit"></i>
                        Modifier
                    </a>
                </td>
                <td>
                    <a class="btn btn-default" href="{{ path('ms_admin_deleteCourse', {'id': course.id}) }}">
                        <i class="glyphicon glyphicon-remove"></i>
                        Supprimer
                    </a>
                </td>
            </tr>
        {% endfor %}

        </tbody>
        </div>
        </table>



    <h2>Matières
        <a class="btn btn-default" href="{{ path('ms_admin_addSubject') }}">
            <i class="glyphicon glyphicon-plus"></i>
            Ajouter
        </a>
    </h2>


        <table class="table table-striped">
            <thead>
                <tr>
                    <th>TYPE</th>
                    <th>MATIERE</th>
                    <th>COEFFICIENT</th>
                    <th>PROFESSEUR</th>
                </tr>
            </thead>
        <div class="row">
        <tbody>
    {% for subject in subjectslist %}
            <tr>
                <td>{{ subject.type }}</td>
                <td>{{ subject.name }}</td>
                <td>{{ subject.coef }}</td>
                <td>

                    <table>
                        {% for teacher in subject.teachers %}
                            <tr>
                                    <td>{{ teacher.firstname }} {{ teacher.lastname }}{% if not loop.last %}, {% endif %}</td>
                            </tr>
                        {% endfor %}
                    </table>
                </td>
                <td>
                    <a class="btn btn-default" href="{{ path('ms_admin_editSubject', {'id': subject.id}) }}">
                        <i class="glyphicon glyphicon-edit"></i>
                        Modifier
                    </a>
                </td>
                <td>
                    <a class="btn btn-default" href="{{ path('ms_admin_deleteSubject', {'id': subject.id}) }}">
                        <i class="glyphicon glyphicon-remove"></i>
                        Supprimer
                    </a>
                </td>
            </tr>
    {% endfor %}
        </tbody>
        </div>
        </table>

</div>
{% endblock %}

